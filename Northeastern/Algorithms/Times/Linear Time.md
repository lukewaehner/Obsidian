# Linear Time - O(n)

Any algorithm which takes time proportional to the input size $n$ (constant factors $C_n$ are okay).

Hard for algorithms to be faster since we usually have to read through the input.

**Examples:**
- Find the maximum or minimum of a given array
- Number of drops needed if we only have a single drone
