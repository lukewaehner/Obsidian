
---
## Notes:
- Any algorithm which takes time proportional to the input size $n \, (C_n \text{ is okay})$
- Hard for algorithms to be faster since we have to read through the input 
	- Example: find the maximum or minimum of a given array
	- Number of drops needed if we only have a single drone
