# Constant Time - O(1)

Time taken $c$ doesn't depend on $n$ the input size. Tends to be very simple operations that build up to more complicated algorithms.

**Examples:**
- Return first character of string
- Perform one addition (x + y)  
- Index into an array (return max/min from a sorted array)

**Note:** Useful algorithms don't have constant time complexity.
